spring:
  cloud:
    config:
      label: master
      uri: http://localhost:9090
      name: config-info
      profile: dev
      backup:
        enabled: true
        fallbackLocation: /use/local/src